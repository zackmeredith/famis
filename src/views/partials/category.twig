{# View :: Partial :: Category #}

{% if post.terms('category')%}
  {% for cat in post.terms('category') %}
    <a class="btn btn-outline btn-grey" href="{{cat.link}}" itemprop="articleSection">{{cat.name}}</a>
  {% endfor %}
{% endif %}
